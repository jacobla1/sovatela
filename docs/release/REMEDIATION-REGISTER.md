# Remediation register — August 2026 external review

Generated: 2026-09-01 · Code commit described: `7d25e1a`
· Public release under review: **v1.6.0** (`4f7e17b`, unchanged)

Generated by `scripts/gen-remediation-register.mjs`. Do not edit by hand — a
hand-edited register drifts from the code it describes, which is how an earlier
revision came to name a commit two behind while still claiming F17 had no hash
verification.

The commit above is the **code** commit this describes. This file is committed
after it, on its own, because a register cannot name the commit that contains it.

This lives in version control rather than as an exported file. The revision
before it was shared as an HTML export, was not regenerated when its contents
changed, and was reviewed in a stale state — which cost a full review round
arguing against claims that had already been withdrawn.

**Read the two status columns separately.** *Public v1.6.0* is what a person can
download today. *Candidate* is what is on this branch. They are different, and
conflating them is how this register once asserted a protection that did not
exist: an earlier revision said v1.6.0 had withdrawn terminal access. It had not
— that was written after the release, in an uncommitted working copy that was
discarded.

| # | Finding | Public v1.6.0 | Candidate | Residual risk | Evidence | Independently re-verified |
|---|---|---|---|---|---|---|
| `F01` | Terminal key in the agent's environment | Present — feature enabled, defect present | Fixed: key exported only into the proxy subshell; agent's environment stripped of provider secrets | None known | verify-launcher.sh; live macOS session; Linux container; Windows CI | Yes — accepted resolved |
| `F02` | Localhost proxy impersonation | Present | Fixed on all three platforms: listener identified before anything authenticated is sent; fails closed when ownership cannot be determined | Ephemeral-port race is not exercised live on any platform; the decision is unit-tested instead | Unix harness (10 checks); Windows CI ownership predicate + ordering assertions | Unix yes at d433757; **Windows fix is newer than the last review** |
| `F03` | Workspace grant via IPC | Present | Fixed: native picker in Rust, canonicalized; renderer can only clear | Guard is a source assertion, not a runtime IPC call | Source assertions | Yes |
| `F04` | Cleartext endpoint tokens | Present | Fixed: HTTPS except loopback, at save, at send, and on every redirect hop | None known | Live redirect test; unit tests incl. port/credential lookalikes | Yes |
| `F05` | History migration not transactional | Present | Fixed: rollback covers collision losers; staging unpredictable, exclusively created, never recursively removed; a cleanup failure is a warning that still saves the new folder, not a failure that hides the moved chats | Cleanup failure leaves older duplicate copies in a named folder, reported not silent | Collision-then-failure test; adopt-and-delete test | First fix yes; **the staging fix is newer than the last review** |
| `F06` | Silent conversation save failure | Present | Fixed: banner, retry, in-memory snapshot, survives chat switches | No warn-before-quit | Component tests that inject write failures | Yes |
| `F07` | Privacy wipe reports false success | Present | Fixed: each removal verified by absence; partial-failure report | None known | Backend + panel tests | Yes |
| `F08` | Destructive IPC relies on renderer confirmation | Present | Two irreversible commands confirm in Rust; single-item deletions do not | **Accepted risk.** Project, memory, template, key and usage deletion are permanent and renderer-confirmed | SECURITY.md acceptance; test pins the boundary | Reclassified as the reviewer asked |
| `F09` | Stale PID kill instructions | Present | Fixed: identity-verified stop everywhere; no PID file written | None known | Source-walking test over all shipped files | Yes |
| `F10` | Unsigned, untested Windows/Linux installers | Present | Disclosure table added; **signing declined** for Windows (recurring cost) and not done for Linux | **Accepted, by decision.** Windows SmartScreen warning is permanent; a signed SHA256SUMS.txt is free and still open | docs/INSTALL.md; SECURITY.md § Release integrity | Yes — recorded as declined, not deferred |
| `F11` | Response and attachment aggregates unbounded | Present | Fixed: response caps, magic-byte typing, per-message budget, Rust bounds at save, list, load and context assembly | None known | Rust + JS tests incl. multibyte truncation | **Load/context bounds are newer than the last review** |
| `F12` | Workspace symlink race | Present | Not fixed. Rationale corrected; warning added before folder selection | **Accepted risk.** Race remains for shared or other-account-writable folders | TECHNICAL-SPEC 7.2 | Rationale accepted; race open |
| `F13` | Storage not durable or transactional | Present | Fixed: unique exclusive temp files, fsync, NotFound distinguished, deletion reordered | Locked files and abrupt termination not simulated | Concurrency test; source assertions | Yes |
| `F14` | Update and pricing input too trusted | Present | Origin/path allowlist, body caps, price-table validation | **Accepted:** release metadata is not independently signed | Unit tests incl. lookalike hosts | Partial, as stated |
| `F15` | Disclosures contradict behaviour | Present | Privacy launch call, Quick Start, scaling, deletion scope, key wording all corrected | **Recurred once, and was found by review rather than by test.** The security note said the app has "no background network activity"; it makes one automatic call. The guard covered three named documents, and this was a fourth | Cross-document tests that assert the code first, now sweeping every tracked file | Partial, as stated — the second round found one more, in a document the guard did not reach |
| `F16` | Accessibility incomplete | Present | Chat-list cause found (WebKit drops a flex item's list role) and changed in 1.6.1; claim not widened — the published 1.6.1 release notes had widened it and were corrected | **Unverified, and no pass scheduled.** No VoiceOver run since the change. NVDA/JAWS/Orca untested | Markup test paired with the flex rule; a sweep that fails any document reporting the change as confirmed | Partial, as stated — and the statement no longer implies a pass is coming |
| `F17` | Installer changes global state | Present | Fixed: uv verified against a hard-coded SHA-256 per platform; 2,417 content hashes installed with --require-hashes; app-owned venv; backups on all three | **Accepted:** we cannot vouch for uv or LiteLLM as projects — verified delivery is not audited code | Windows CI install checks; Linux CI end-to-end and tamper checks (**written, not yet run**) | Windows yes; **Linux CI job is new and has not run** |
| `F18` | Legal publication unresolved | Present | **Closed by decision 2026-09-01**, not by review: the sections that needed a lawyer were the ones limiting liability, so they were removed rather than published unreviewed. Short terms published at /terms. Narrowed again on a second reading: §4 no longer allocates the cost of a defect to the user, §5 restores the provider clause on output ownership, and the version stamp is an effective date with the page attached to each release | Pre-contract information duties for free software remain unanswered — but nothing published depends on the answer, and §1/§4 are facts that stay true either way | Release-hygiene tests now guard the decision, not a due date | Yes — recorded as decided, with what was dropped and why |
| `F19` | Binary notices and provenance | Present | Manifest generated (608 crates, 151 packages), bundled, in-app link | **Not done:** signed provenance; mutable runner and toolchain refs | tests/thirdPartyNotices.test.js | Partial, as stated |
| `F20` | Website response headers | Present | Meta CSP and no-referrer on both templates | **Accepted:** nosniff and frame-ancestors need response headers GitHub Pages cannot set | tests/siteHeaders.test.js | Partial, as stated |
| `+01` | Settings read failure treated as fresh install | Present | Fixed: NotFound distinguished from a read error | None known | Rust tests | Found during remediation, not in the review |

## Standing facts

- **The terminal-access defects (`F01`, `F02`, `F09`) are present in 1.2.0
  through 1.6.0**, which is every release that shipped the `claude-glm`
  integration. **The other findings are present in the public v1.6.0 release**
  unless a row says otherwise. Those releases stay as published; the fixes are in
  **v1.6.1**, released 2026-09-01.
- **Installing 1.6.1 repairs nothing by itself.** The launcher sits outside the
  application and is not replaced until setup is run again, and a key that was in
  an agent's environment stays exposed whatever is installed afterwards. The
  remediation is manual, and it is the same for everyone who used the feature.
- **The affected population is unknown.** The application has no telemetry, so
  there is no way to establish who installed terminal access. There is **no known
  exploitation**, which is not the same as no exploitation.
- **Disclosed 2026-09-01.** The security note is published at
  <https://sovatela.eu/security-note-claude-glm>, the download page carries it
  above the downloads, and `version.json` points *Check for updates* at it — which
  reaches 1.5.0 and later only, since the update check did not exist before 1.5.0.
  The advisory is
  [GHSA-jpv9-3mvc-5v5c](https://github.com/jacobla1/sovatela/security/advisories/GHSA-jpv9-3mvc-5v5c),
  CVSS 6.3, with a CVE requested and not yet assigned.
- **Verification for 1.6.1 is recorded in
  [`QA-1.6.1.md`](QA-1.6.1.md)**, including the checks that were not run and why.
  The delivery route is verified by `scripts/verify-update-route.sh`, which
  compiles the update comparison out of the **v1.6.0 tag** and runs it against the
  live manifest rather than reasoning about it.
- Accepted risks are recorded with their trade in
  [`TECHNICAL-SPEC.md` § 7.2](../TECHNICAL-SPEC.md#72-accepted-risks-from-the-august-2026-review).
